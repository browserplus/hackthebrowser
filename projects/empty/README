# Empty Project

This is an empty project.  The syntax of this file is MarkDown.  Markdown
is a TEXT -> HTML format that keeps your text files looking like text
files, but gives you rich formatting options.  For more information,
check out [PHP Markdown](http://michelf.com/projects/php-markdown/extra/).

1. a numbered list
2. can be started
3. by typing numbers

* a bullet list
* can be started
* by typing asteriks

But don't forget to [link](http://www.github.com/) your project.  
~~~

function dbg($str) {
    error_log("$str\n", 3, "/tmp/dbg.txt");
}

~~~

That is all, for now.